# Movie-Recommendation-System
Movie recommender system using content-based filtering. TF-IDF vectorizes movie overviews and genres from dataset.csv, then applies cosine similarity to find similar movies. Includes preprocess.py script and precomputed similarity matrix.
